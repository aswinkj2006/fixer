# fixer.ai — Python dependencies
# Core backend
fastapi==0.115.0
uvicorn[standard]==0.30.6
python-multipart==0.0.9
websockets==13.1

# Database
sqlalchemy==2.0.35
aiosqlite==0.20.0

# Vector DB
chromadb==0.5.20

# HTTP client (for manual downloads, Ollama API)
httpx==0.27.2
requests==2.32.3

# PDF parsing
PyMuPDF==1.24.11

# HTML parsing
beautifulsoup4==4.12.3
lxml==5.3.0

# Sensor simulation math
numpy==1.26.4
scipy==1.14.1

# ML / clustering (recurring fault detection)
scikit-learn==1.5.2

# Slack
slack-bolt==1.21.1

# Speech-to-text (deferred — install when switching hardware)
# faster-whisper==1.0.3

# LLM (deferred — install when switching to better hardware)
# ollama==0.3.3  # Python client for Ollama

# Environment
python-dotenv==1.0.1

# Testing
pytest==8.3.3
pytest-asyncio==0.24.0
httpx==0.27.2  # also used for async test client
